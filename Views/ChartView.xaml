<UserControl x:Class="WpfApplication1.Views.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApplication1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="377" d:DesignWidth="1820">
    <Grid>
        <Canvas HorizontalAlignment="Left" Height="377" Margin="0,0,0,0" VerticalAlignment="Top" Width="1820">
            <Canvas.Resources>
                <Style TargetType="{x:Type ToggleButton}">
                    <Setter Property="Command" Value="{Binding ToggleCommand}" />
                    <Setter Property="Opacity" Value="0.2" />
                    <Setter Property="Width" Value="40" />
                    <Setter Property="Height" Value="40" />
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Opacity" Value="0.2" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Canvas.Resources>

            <Image x:Name="image" Height="367" Canvas.Left="10" Width="1800" Source="../Resources/table.png" Canvas.Top="2"/>
            <ToggleButton x:Name="btn0" CommandParameter="E:6:0" Content="E" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="318" />
            <ToggleButton x:Name="btn1" CommandParameter="A:5:0" Content="A" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="256" />
            <ToggleButton x:Name="btn2" CommandParameter="D:4:0" Content="D" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="196" />
            <ToggleButton x:Name="btn3" CommandParameter="G:3:0" Content="G" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="134" />
            <ToggleButton x:Name="btn4" CommandParameter="B:2:0" Content="B" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="78" />
            <ToggleButton x:Name="btn5" CommandParameter="E:1:0" Content="E" IsChecked="{Binding Chart.Note_E}" Canvas.Left="25" Canvas.Top="14"/>
        </Canvas>

    </Grid>
</UserControl>
